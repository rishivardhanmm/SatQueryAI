# Optional live inference gateway. Set these as hosted runtime secrets for Sites.
INFERENCE_URL=
MODEL_API_KEY=
# Optional docker-compose infrastructure, not used by the hosted D1/R2 app.
POSTGRES_PASSWORD=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
